<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reporte de Conceptos por Turnos</title>
    <style>
        /* Plantilla CSS para reporte de conceptos por turnos */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 20px;
            color: #333;
            line-height: 1.4;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #2c5282;
            padding-bottom: 20px;
        }

        .hotel-name {
            font-size: 24px;
            font-weight: bold;
            color: #2c5282;
            margin: 0;
        }

        .hotel-info {
            font-size: 12px;
            color: #666;
            margin-top: 5px;
        }

        .report-title {
            font-size: 20px;
            font-weight: bold;
            margin: 20px 0 10px 0;
            text-align: center;
        }

        .report-subtitle {
            font-size: 14px;
            color: #666;
            text-align: center;
            margin-bottom: 20px;
        }

        .metadata {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            padding: 10px;
            background-color: #f7fafc;
            border-radius: 4px;
            font-size: 12px;
        }

        .section {
            margin-bottom: 30px;
            page-break-inside: avoid;
        }

        .section-title {
            font-size: 16px;
            font-weight: bold;
            color: #2c5282;
            margin-bottom: 15px;
            border-bottom: 1px solid #e2e8f0;
            padding-bottom: 5px;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .summary-card {
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 15px;
            background-color: #ffffff;
        }

        .summary-card h3 {
            margin: 0 0 10px 0;
            font-size: 14px;
            color: #2c5282;
            font-weight: bold;
        }

        .summary-totals {
            margin-top: 10px;
        }

        .currency-total {
            display: flex;
            justify-content: space-between;
            margin: 5px 0;
            font-size: 12px;
        }

        .currency-total.positive {
            color: #38a169;
        }

        .currency-total.negative {
            color: #e53e3e;
        }

        .movements-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
            font-size: 11px;
        }

        .movements-table th {
            background-color: #2c5282;
            color: white;
            padding: 8px 4px;
            text-align: left;
            font-weight: bold;
        }

        .movements-table td {
            padding: 6px 4px;
            border-bottom: 1px solid #e2e8f0;
        }

        .movements-table tr:nth-child(even) {
            background-color: #f7fafc;
        }

        .amount {
            text-align: right;
            font-weight: bold;
        }

        .amount.income {
            color: #38a169;
        }

        .amount.expense {
            color: #e53e3e;
        }

        .grand-totals {
            background-color: #2c5282;
            color: white;
            padding: 20px;
            border-radius: 8px;
            margin-top: 30px;
        }

        .grand-totals h3 {
            margin: 0 0 15px 0;
            text-align: center;
        }

        .grand-totals-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            text-align: center;
        }

        .grand-total-item {
            background-color: rgba(255, 255, 255, 0.1);
            padding: 10px;
            border-radius: 4px;
        }

        .grand-total-currency {
            font-weight: bold;
            font-size: 14px;
            margin-bottom: 8px;
        }

        .grand-total-amount {
            font-size: 18px;
            font-weight: bold;
        }

        .footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #e2e8f0;
            text-align: center;
            font-size: 10px;
            color: #666;
        }

        .signatures {
            display: flex;
            justify-content: space-around;
            margin-top: 60px;
        }

        .signature-box {
            text-align: center;
            width: 200px;
        }

        .signature-line {
            border-bottom: 1px solid #333;
            margin-bottom: 5px;
            height: 40px;
        }

        /* Estilos para impresión */
        @media print {
            body {
                margin: 0;
                padding: 15px;
            }
            
            .section {
                page-break-inside: avoid;
            }
            
            .movements-table {
                font-size: 9px;
            }
            
            .movements-table th,
            .movements-table td {
                padding: 4px 2px;
            }
        }

        /* Utilidades */
        .text-center { text-align: center; }
        .text-right { text-align: right; }
        .font-bold { font-weight: bold; }
        .text-small { font-size: 10px; }
        .mb-10 { margin-bottom: 10px; }
        .mb-20 { margin-bottom: 20px; }

        /* Estados de turno */
        .turno-badge {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 12px;
            font-size: 10px;
            font-weight: bold;
            color: white;
        }

        .turno-1 { background-color: #4299e1; }
        .turno-2 { background-color: #48bb78; }
        .turno-3 { background-color: #ed8936; }
        .turno-0 { background-color: #a0aec0; }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header">
        <h1 class="hotel-name">{{hotelName}}</h1>
        <div class="hotel-info">
            {{#if address}}{{address}}<br>{{/if}}
            {{#if phone}}Tel: {{phone}} {{/if}}
            {{#if email}}| Email: {{email}}{{/if}}
        </div>
    </div>

    <!-- Report Title -->
    <h2 class="report-title">Reporte de Conceptos por Turnos</h2>
    <div class="report-subtitle">
        Período: {{formatDate dateRange.from}} - {{formatDate dateRange.to}}
        {{#if selectedTurnos}}| Turnos: {{join selectedTurnos ", "}}{{/if}}
    </div>

    <!-- Metadata -->
    <div class="metadata">
        <div>
            <strong>Generado por:</strong> {{generatedBy}}<br>
            <strong>Fecha de generación:</strong> {{formatDateTime metadata.generatedAt}}
        </div>
        <div>
            <strong>Total de registros:</strong> {{metadata.totalRecords}}<br>
            <strong>Período:</strong> {{daysBetween dateRange.from dateRange.to}} días
        </div>
    </div>

    <!-- Summary Section -->
    <div class="section">
        <h3 class="section-title">Resumen por Turnos</h3>
        <div class="summary-grid">
            {{#each summary}}
            <div class="summary-card">
                <h3>
                    <span class="turno-badge turno-{{turno}}">Turno {{turno}}</span>
                    {{turnoName}}
                </h3>
                
                <div class="summary-totals">
                    <strong>Totales:</strong>
                    {{#each totals}}
                    {{#if @this.income}}
                    <div class="currency-total">
                        <span>{{@key}}:</span>
                        <span>
                            <span class="income">+{{formatCurrency @this.income @key}}</span> | 
                            <span class="expense">-{{formatCurrency @this.expenses @key}}</span> = 
                            <span class="{{#if (gt @this.net 0)}}positive{{else}}negative{{/if}}">
                                {{formatCurrency @this.net @key}}
                            </span>
                        </span>
                    </div>
                    {{/if}}
                    {{/each}}
                </div>
                
                <div style="margin-top: 10px; font-size: 11px; color: #666;">
                    <div>Movimientos: {{totalMovements}}</div>
                    <div>Pagos: {{totalPayments}} | Reembolsos: {{totalRefunds}}</div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <!-- Detailed Movements -->
    {{#if config.includeDetails}}
    <div class="section">
        <h3 class="section-title">Detalle de Movimientos</h3>
        
        {{#if (eq config.groupBy "turno")}}
            {{#each (groupByTurno movements)}}
            <div style="margin-bottom: 25px;">
                <h4 style="color: #2c5282; margin-bottom: 10px;">
                    <span class="turno-badge turno-{{@key}}">Turno {{@key}}</span>
                    {{#each @this}}{{#if @first}}{{turno.nombre}}{{/if}}{{/each}}
                </h4>
                
                <table class="movements-table">
                    <thead>
                        <tr>
                            <th>Fecha/Hora</th>
                            <th>Tipo</th>
                            <th>Concepto</th>
                            <th>Huésped/Reserva</th>
                            <th>Usuario</th>
                            <th>Referencia</th>
                            <th>Moneda</th>
                            <th>Monto</th>
                            <th>Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each @this}}
                        <tr>
                            <td>{{formatDateTime date}}</td>
                            <td>{{formatMovementType type}}</td>
                            <td>{{concept}}</td>
                            <td>
                                {{#if booking}}
                                {{booking.guestName}}<br>
                                <span class="text-small">#{{booking.id}}</span>
                                {{/if}}
                            </td>
                            <td>
                                {{#if user}}
                                {{user.name}} {{user.lastName}}
                                {{else}}
                                <span class="text-small">Sistema</span>
                                {{/if}}
                            </td>
                            <td>{{reference}}</td>
                            <td>{{currency}}</td>
                            <td class="amount {{#if isIncome}}income{{else}}expense{{/if}}">
                                {{#if isIncome}}+{{else}}-{{/if}}{{formatCurrency amount currency}}
                            </td>
                            <td class="amount {{#if isIncome}}income{{else}}expense{{/if}}">
                                {{#if isIncome}}+{{else}}-{{/if}}{{formatCurrency total currency}}
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            {{/each}}
        {{else}}
            <!-- Vista sin agrupar por turno -->
            <table class="movements-table">
                <thead>
                    <tr>
                        <th>Fecha/Hora</th>
                        <th>Turno</th>
                        <th>Tipo</th>
                        <th>Concepto</th>
                        <th>Huésped/Reserva</th>
                        <th>Usuario</th>
                        <th>Moneda</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each movements}}
                    <tr>
                        <td>{{formatDateTime date}}</td>
                        <td>
                            {{#if turno}}
                            <span class="turno-badge turno-{{turno.numero}}">{{turno.numero}}</span>
                            {{else}}
                            <span class="turno-badge turno-0">N/A</span>
                            {{/if}}
                        </td>
                        <td>{{formatMovementType type}}</td>
                        <td>{{concept}}</td>
                        <td>
                            {{#if booking}}
                            {{booking.guestName}}<br>
                            <span class="text-small">#{{booking.id}}</span>
                            {{/if}}
                        </td>
                        <td>
                            {{#if user}}
                            {{user.name}} {{user.lastName}}
                            {{else}}
                            <span class="text-small">Sistema</span>
                            {{/if}}
                        </td>
                        <td>{{currency}}</td>
                        <td class="amount {{#if isIncome}}income{{else}}expense{{/if}}">
                            {{#if isIncome}}+{{else}}-{{/if}}{{formatCurrency total currency}}
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        {{/if}}
    </div>
    {{/if}}

    <!-- Grand Totals -->
    {{#if config.showTotals}}
    <div class="grand-totals">
        <h3>Totales Generales</h3>
        <div class="grand-totals-grid">
            {{#each grandTotals}}
            {{#if @this.income}}
            <div class="grand-total-item">
                <div class="grand-total-currency">{{@key}}</div>
                <div style="font-size: 12px; margin-bottom: 5px;">
                    Ingresos: {{formatCurrency @this.income @key}}<br>
                    Egresos: {{formatCurrency @this.expenses @key}}
                </div>
                <div class="grand-total-amount">
                    {{#if (gt @this.net 0)}}+{{/if}}{{formatCurrency @this.net @key}}
                </div>
            </div>
            {{/if}}
            {{/each}}
        </div>
    </div>
    {{/if}}

    <!-- Signatures -->
    <div class="signatures">
        <div class="signature-box">
            <div class="signature-line"></div>
            <div>Recepcionista</div>
        </div>
        <div class="signature-box">
            <div class="signature-line"></div>
            <div>Supervisor</div>
        </div>
        <div class="signature-box">
            <div class="signature-line"></div>
            <div>Gerencia</div>
        </div>
    </div>

    <!-- Footer -->
    <div class="footer">
        <p>Este reporte fue generado automáticamente por el Sistema de Gestión Hotelera</p>
        <p>Página generada el {{formatDateTime metadata.generatedAt}} | Confidencial</p>
    </div>
</body>
</html>
