<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reporte de Conceptos por Turnos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            font-size: 12px;
            line-height: 1.4;
            color: #333;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #3b82f6;
            padding-bottom: 20px;
        }

        .hotel-name {
            font-size: 24px;
            font-weight: bold;
            color: #1e40af;
            margin-bottom: 5px;
        }

        .hotel-info {
            font-size: 12px;
            color: #6b7280;
            margin-bottom: 20px;
        }

        .report-title {
            font-size: 18px;
            font-weight: bold;
            color: #374151;
            margin-bottom: 10px;
        }

        .report-period {
            font-size: 14px;
            color: #6b7280;
            margin-bottom: 5px;
        }

        .metadata {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            font-size: 11px;
            color: #6b7280;
        }

        .turn-section {
            margin-bottom: 30px;
            page-break-inside: avoid;
        }

        .turn-header {
            background-color: #f3f4f6;
            border: 1px solid #d1d5db;
            padding: 15px;
            margin-bottom: 10px;
            border-radius: 5px;
        }

        .turn-title {
            font-size: 16px;
            font-weight: bold;
            color: #1f2937;
            margin-bottom: 5px;
        }

        .turn-info {
            display: flex;
            justify-content: space-between;
            font-size: 12px;
            color: #6b7280;
        }

        .concepts-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 15px;
            border: 1px solid #d1d5db;
        }

        .concepts-table th {
            background-color: #f9fafb;
            border: 1px solid #d1d5db;
            padding: 10px;
            text-align: left;
            font-weight: bold;
            font-size: 11px;
            color: #374151;
        }

        .concepts-table td {
            border: 1px solid #d1d5db;
            padding: 8px;
            font-size: 11px;
        }

        .concepts-table tbody tr:nth-child(even) {
            background-color: #f9fafb;
        }

        .amount {
            text-align: right;
            font-weight: bold;
        }

        .positive-amount {
            color: #059669;
        }

        .negative-amount {
            color: #dc2626;
        }

        .turn-summary {
            background-color: #fef3c7;
            border: 1px solid #f59e0b;
            padding: 15px;
            border-radius: 5px;
            margin-top: 10px;
        }

        .summary-title {
            font-weight: bold;
            margin-bottom: 10px;
            color: #92400e;
        }

        .summary-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .summary-item {
            display: flex;
            justify-content: space-between;
            padding: 5px 0;
            border-bottom: 1px solid #f59e0b;
        }

        .summary-item:last-child {
            border-bottom: none;
            font-weight: bold;
            font-size: 13px;
        }

        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 10px;
            color: #9ca3af;
            border-top: 1px solid #e5e7eb;
            padding-top: 20px;
        }

        .no-data {
            text-align: center;
            color: #6b7280;
            font-style: italic;
            padding: 20px;
        }

        @media print {
            body {
                margin: 0;
                padding: 15px;
            }
            
            .turn-section {
                page-break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <!-- Header del reporte -->
    <div class="header">
        <div class="hotel-name"><%= hotel.name %></div>
        
        <% if (hotel.address) { %>
        <div class="hotel-info">
            <%= hotel.address %>
            <% if (hotel.phone) { %> • Tel: <%= hotel.phone %><% } %>
            <% if (hotel.email) { %> • <%= hotel.email %><% } %>
        </div>
        <% } %>

        <div class="report-title">Reporte de Conceptos por Turnos</div>
        <div class="report-period">
            Período: <%= formatDate(reportData.startDate) %> - <%= formatDate(reportData.endDate) %>
        </div>
    </div>

    <!-- Metadata del reporte -->
    <div class="metadata">
        <div>
            <strong>Generado por:</strong> <%= generatedBy %><br>
            <strong>Fecha de generación:</strong> <%= formatDateTime(generatedAt) %>
        </div>
        <div>
            <strong>Total de turnos:</strong> <%= reportData.turns.length %><br>
            <strong>Moneda:</strong> <%= formatCurrency(reportData.currency) %>
        </div>
    </div>

    <!-- Contenido principal -->
    <% reportData.turns.forEach(function(turn) { %>
    <div class="turn-section">
        <!-- Header del turno -->
        <div class="turn-header">
            <div class="turn-title"><%= turn.name %></div>
            <div class="turn-info">
                <span><strong>Fecha:</strong> <%= formatDate(turn.startDate) %></span>
                <span><strong>Horario:</strong> <%= formatTime(turn.startTime) %> - <%= formatTime(turn.endTime) %></span>
                <span><strong>Personal:</strong> <%= turn.staff.length %> empleado(s)</span>
            </div>
        </div>

        <!-- Conceptos del turno -->
        <% if (turn.concepts.length > 0) { %>
        <table class="concepts-table">
            <thead>
                <tr>
                    <th style="width: 40%;">Concepto</th>
                    <th style="width: 15%;">Categoría</th>
                    <th style="width: 15%;">Tipo</th>
                    <th style="width: 15%;">Cantidad</th>
                    <th style="width: 15%;">Monto</th>
                </tr>
            </thead>
            <tbody>
                <% turn.concepts.forEach(function(concept) { %>
                <tr>
                    <td><%= concept.name %></td>
                    <td><%= concept.category %></td>
                    <td><%= concept.type %></td>
                    <td class="amount"><%= concept.quantity %></td>
                    <td class="amount <%= isPositive(concept.amount) ? 'positive-amount' : 'negative-amount' %>">
                        <%= formatMoney(concept.amount, reportData.currency) %>
                    </td>
                </tr>
                <% }); %>
            </tbody>
        </table>
        <% } else { %>
        <div class="no-data">No hay conceptos registrados para este turno</div>
        <% } %>

        <!-- Resumen del turno -->
        <div class="turn-summary">
            <div class="summary-title">Resumen del Turno</div>
            <div class="summary-grid">
                <div>
                    <div class="summary-item">
                        <span>Ingresos:</span>
                        <span class="positive-amount"><%= formatMoney(turn.totalIncome, reportData.currency) %></span>
                    </div>
                    <div class="summary-item">
                        <span>Gastos:</span>
                        <span class="negative-amount"><%= formatMoney(turn.totalExpenses, reportData.currency) %></span>
                    </div>
                </div>
                <div>
                    <div class="summary-item">
                        <span>Total de conceptos:</span>
                        <span><%= turn.concepts.length %></span>
                    </div>
                    <div class="summary-item">
                        <span>Balance neto:</span>
                        <span class="<%= isPositive(turn.netBalance) ? 'positive-amount' : 'negative-amount' %>">
                            <%= formatMoney(turn.netBalance, reportData.currency) %>
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <% }); %>

    <!-- Footer -->
    <div class="footer">
        <p>Este reporte fue generado automáticamente por el Sistema de Gestión Hotelera.</p>
        <p>Para más información, contacte al administrador del sistema.</p>
    </div>
</body>
</html>
